# Agents

## Cursor Cloud specific instructions

This is a greenfield repository named **Artwork-Detect**. As of initial setup, the repo contains only a `README.md` file with no source code, dependencies, build system, or services.

### Environment

- Python 3.12 and Node.js are available on the VM.
- No dependencies or services are required at this time.

### When code is added

Once application code and dependency files (e.g. `requirements.txt`, `package.json`, `pyproject.toml`) are committed, update the VM update script accordingly via `SetupVmEnvironment` and expand this section with lint/test/build/run commands.
